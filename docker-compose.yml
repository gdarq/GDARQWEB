version: '3.8'

services:
  portfolio:
    build: .
    container_name: gdestudio-portfolio
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
